{
  "name": "@app-deporte/shared",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"No shared linter configured yet\"",
    "typecheck": "tsc --noEmit",
    "test": "echo \"No shared tests configured yet\""
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}

